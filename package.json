{
  "name": "sitaratas",
  "private": true,
  "workspaces": [
    "server",
    "engine",
    "web"
  ],
  "scripts": {
    "dev": "npm run dev -w server",
    "dev:web": "npm run dev -w web",
    "build": "npm run build -w engine && npm run build -w web && npm run build -w server",
    "start": "npm start -w server",
    "test": "npm test -w engine"
  }
}
